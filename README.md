#Instalar as bibliotecas
pip install fastapi uvicorn sqlalchemy alembic python-dotenv

#Rodar o fastapi
no terminal: python -m uvicorn --reload

#Acessar a documntação
acesse no navegador: http://127.0.0.1:8000
